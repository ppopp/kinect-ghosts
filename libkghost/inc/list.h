#ifndef _list_h_
#define _list_h_

#include "common.h"
#include <stddef.h>

#ifdef __cplusplus
extern "C" {
#endif

	/** \addtogroup list 
	 * @{
	 */

	/*
	typedef struct list_s* list_handle_t;
	typedef struct list_entry_s* list_iterator_t;

	status_t list_create(size_t element_size, list_handle_t* p_handle);
	void list_release(list_handle_t handle);

	status_t list_count(list_handle_t handle, size_t* p_count);
	status_t list_begin(list_handle_t handle, list_iterator_t* p_first);
	status_t list_next(list_iterator_t* p_iterator);
	*/
	



	/** @} */

#ifdef __cplusplus
}
#endif

#endif

